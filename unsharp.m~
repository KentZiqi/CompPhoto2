function image = unsharp(gamma,hsize,image_name)

figure
I = im2double(imread(image_name));
h = fspecial('gaussian', 10, 100);
J = imfilter(I,h);
subplot(1,3,1)
imshow(I)
subplot(1,3,2)
imshow(J)
subplot(1,3,3)
imshow(I+0.5*(I-J))
end